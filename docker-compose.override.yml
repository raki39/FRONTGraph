services:
  api:
    volumes:
      - ./api:/app/api
      - ./agentgraph:/app/agentgraph
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

  worker:
    volumes:
      - ./agentgraph:/app/agentgraph


